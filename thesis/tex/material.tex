%\enableregime[utf]  % use UTF-8

\setupcolors[state=start]
\setupinteraction[state=start, color=blue] % needed for hyperlinks

\usemodule[simplefonts]
\setmainfont[Liberation-Serif]
\setmonofont[inconsolata]

\setuppapersize[A4][A4]  % use letter paper
%\setuplayout[width=middle, backspace=1.5in, cutspace=1.5in,
%             height=middle, header=0.75in, footer=0.75in] % page layout
\setuppagenumbering[location={footer,right}]  % number pages
\setupbodyfont[12pt]  % 11pt font
\setupwhitespace[small]  % inter-paragraph spacing

\setupindenting[medium]
\indenting[always]

\setuphead[section][style=\tfc]
\setuphead[subsection][style=\tfb]
\setuphead[subsubsection][style=\bf]

% define descr (for definition lists)
\definedescription[descr][
  headstyle=bold,style=normal,align=left,location=hanging,
  width=broad,margin=1cm]

% prevent orphaned list intros
\setupitemize[autointro]

% define defaults for bulleted lists 
\setupitemize[1][symbol=1][indentnext=no]
\setupitemize[2][symbol=2][indentnext=no]
\setupitemize[3][symbol=3][indentnext=no]
\setupitemize[4][symbol=4][indentnext=no]

\setupthinrules[width=15em]  % width of horizontal rules

% for block quotations
\unprotect

\startvariables all
blockquote: blockquote
\stopvariables

\definedelimitedtext
[\v!blockquote][\v!quotation]

\setupdelimitedtext
[\v!blockquote]
[\c!left=,
\c!right=,
before={\blank[medium]},
after={\blank[medium]},
]

\protect

\starttext

\subject{Material}

\subsubject{Elements of essays}

\startitemize
\item
  Formats (MLA, APA, Chicago, etc.)
  {\em [European examples? Screen-specific examples?]}
\item
  Annotations (Static (page) vs Dynamic (screen)
\stopitemize

\subsubject{Elements of texts}

\subsubsubject{Surface Level}

\startitemize
\item
  Hypertextuality (formal or semantic)
  {\em [Can a claim be made that Hayles' printed hypertext is semantic? could there be a parallel connection between the surface and subtext layers?]}
\item
  Typographic qualities (\quotation{simple issues} of kerning, line
  breaking; \quotation{society} formats)
\stopitemize

\subsubsubject{Subtext Level}

\startitemize
\item
  \quote{Setting standards} (WYSIWYGs and markups as
  \quotation{hairy objects})
  \startitemize
  \item
    markups are sub-divided into formal (TeX) and semantic (HTML)
  \item
    the file won't automatically look the way you want it (Office on
    different platforms, OO.othe same; the \quote{tweak} factor of TeX;
    browser specific line and character spacing)
  \stopitemize
\item
  Display-ability (file formats; DRM; the display itself)
\stopitemize

\subsubsubject{Substrate Level}

\startitemize
\item
  hardware (rarely discussed, barely understood)
  \startitemize
  \item
    but perhaps not necessarily all that {\em relevant} to the material
    analysis of text?
  \stopitemize
\item
  the unspoken labor of {\em making} that hardware
  \startitemize
  \item
    Pasquinelli's parasitic resource extraction
  \stopitemize
\stopitemize

\stoptext

